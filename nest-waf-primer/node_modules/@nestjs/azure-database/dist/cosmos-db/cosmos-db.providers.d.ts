import { Database } from '@azure/cosmos';
export interface PartitionKeyValues {
    PartitionKey: string;
}
export declare function createAzureCosmosDbProviders(connectionName?: string, models?: {
    dto: any;
    collection?: string;
}[]): {
    provide: string;
    useFactory: (database: Database) => Promise<import("@azure/cosmos").Container>;
    inject: string[];
}[];
